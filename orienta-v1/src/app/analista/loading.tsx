import { PageLoader } from "@/components/ui/loading";

export default function AnalistaLoading() {
  return <PageLoader title="Carregando workbench do analista" description="Buscando formularios, recomendacoes e maturidade…" />;
}
