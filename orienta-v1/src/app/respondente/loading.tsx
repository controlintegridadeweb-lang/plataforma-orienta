import { PageLoader } from "@/components/ui/loading";

export default function RespondenteLoading() {
  return <PageLoader title="Carregando seu painel" description="Buscando seus formularios, evidencias e plano de acao…" />;
}
